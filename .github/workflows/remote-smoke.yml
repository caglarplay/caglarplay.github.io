name: Remote Build Smoke
on:
  push:
    branches: [tv-klima-kumanda]
  workflow_dispatch:
jobs:
  smoke:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      - run: echo RUNNER_OK
